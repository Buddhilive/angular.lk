# Dummy MCP Server

This is a dummy Model Context Protocol (MCP) server project for Angular Language Kit.

## Usage

- `npm install` to install dependencies
- `npm run build` to compile TypeScript
- `npm start` to run the server
